# 更新日志

本项目所有值得记录的变更都列在这里。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased] - 0.4.0（验收中）

当前预览包：`@jiangdaoli/dsh-archived-sessions@0.4.0-accept.1`（本地 tarball 安装，验收通过后发布正式版 0.4.0）

### 新增

- **设置导航自定义图标**：`settings.section` 注册只支持 `id/order/label`，未知 id 的导航项固定是齿轮。本版沿用第三方设置页面的做法，给「已归档会话」的导航按钮打标记并用 CSS mask 覆盖齿轮，换成归档箱图标（`index.ts` 的 `adoptSettingsNavGlyph` + `styles.ts`）。不改 DSH 契约，只在 UI 层做展示替换。

## [0.3.1] - 2026-08-27

### 变更

- 归档列表布局重构，分区不再拥挤：
  - **筛选栏**独立分组：标题 / 工作区双输入框改为 grid 双列**等宽左右排列**（任何宽度下不会堆叠），去掉输入框内图标；
  - **分页**移至底部工具栏：页码信息居左、上一页 / 下一页居右，以上边框与列表分隔；
  - 计数栏的「刷新」按钮右对齐。

## [0.3.0] - 2026-08-27

### 新增

- **列表分页**：归档列表每页 10 条，底部提供上一页 / 下一页和「第 x / y 页」指示；删除或恢复会话后页码自动校正，不会停留在空页。
- **标题 / 工作区双筛选**：列表上方新增「搜索标题」与「搜索工作区」两个输入框（可同时生效，AND 关系）。输入即时过滤并回到第 1 页；无匹配时显示「没有匹配的会话」。

## [0.2.3] - 2026-08-22

### 新增

- 会话详情弹窗页脚新增「恢复」按钮，与列表行内按钮等效（修复「恢复入口不易发现」问题，issue #1）。

## [0.2.2] - 2026-08-21

### 修复

- 发布通道修复（改为直发），无功能变化。

## [0.2.1] - 2026-08-21

### 修复

- 0.2.0 撤回后的重新发布，无功能变化。

## [0.2.0] - 2026-08-21

### 新增

- **恢复归档会话**：两步确认，把归档会话取消归档、回到侧边栏活跃列表的原位置。DSH 本身没有取消归档 API，本插件通过 workspace 存储域（`storageDomain`）把会话 id 从 `archivedSessionIds` 移除——与产品归档共用同一条持久化写入链，改动实时生效、重启后保持。

## [0.1.1] - 2026-08-19

### 新增

- 初始版本：
  - 归档会话列表（标题 / 工作区 / 消息数 / 最后活跃时间，支持刷新）；
  - 只读对话查看器（按轮次分组，推理与工具执行折叠展示，Markdown 与表格渲染）；
  - 删除归档记录（墓碑实现：只移除归档记录，会话数据保留在磁盘）。
